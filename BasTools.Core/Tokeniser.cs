using System;
using System.Collections.Generic;
using System.Text;

namespace BasTools.Core
{
    internal class Tokeniser
    {
        public static byte[] TokeniseLine(string text)
        {
            List<byte> bytes = new();

            //int b;
            //while ((b = source.ReadByte()) != -1)
            //{
            //    bytes.Add((byte)b);
            //}

            return bytes.ToArray();
        }
    }
}